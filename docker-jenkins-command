docker run -d `
  --name my-jenkins `
  -p 8080:8080 `
  -p 50000:50000 `
  -v install-jenkins-docker_jenkins-data:/var/jenkins_home `
  -v install-jenkins-docker_jenkins-docker-certs:/certs/client:ro `
  -v /var/run/docker.sock:/var/run/docker.sock `
  jenkins/jenkins:lts